# AI 自动化边界

AI 可以对合成或获授权内容做 OCR、实体候选抽取、案件摘要、字段草稿和解释文本；这些输出始终是建议。

以下事项必须由确定性校验与有权限的人工完成：编号精确匹配、PIT/权限校验、Case 状态迁移、标签确认、账户或资金限制、STR 判断与任何监管外发。高分 label-0、Graph 关联或异常邮件均只表示 `INCONCLUSIVE`，不得自动改标、处罚、冻结、转账或备案。

内部 STR 候选存放在隔离 mart；普通客户画像不暴露案件细节。Demo 没有生产凭证、外部 API、自动执法或自动报送能力。
